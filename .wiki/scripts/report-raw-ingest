#!/usr/bin/env node
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { createRawIngestReport, parseToolOptions, resolveToolLanguage } from './_wiki-tools.mjs';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const options = parseToolOptions(process.argv.slice(2));
const language = resolveToolLanguage(root, options);

process.stdout.write(createRawIngestReport({ root, language }));
